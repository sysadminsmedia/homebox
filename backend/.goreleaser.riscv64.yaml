version: 2

project_name: homebox

# Architecture-specific config for riscv64
# Make sure to check the documentation at https://goreleaser.com
before:
  hooks:
    - go generate ./...
builds:
  - main: ./app/api
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - freebsd
    goarch:
      - riscv64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
    tags:
      - >-
        {{- if eq .Arch "riscv64" }}nodynamic
        {{- else if eq .Os "freebsd" }}nodynamic
        {{ end }}

archives:
  - formats: [ 'tar.gz' ]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
sboms:
  - artifacts: archive
checksum:
  name_template: 'checksums-riscv64.txt'
snapshot:
  version_template: "{{ incpatch .Version }}-next"
changelog:
  disable: true
release:
  disable: true

# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj
